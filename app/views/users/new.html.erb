<%# 新規登録フォームを作成 %>

<%= form_with model: @user, url: users_path do |f| %>
    <p>ユーザー名</p>
    <%= f.text_field :name%>
    <p>メールアドレス </p>
    <%= f.text_field :email%>
    <br>
    <br>
    <%= f.submit value = "登録する"%>
<% end %>