<%# 投稿編集ページ %>

<%= form_with model: @post, url: post_path do |f| %>

    <% @post.errors.full_messages.each do |message|%> %>
        <%= message %>
    <% end %>

    <%= f.text_area :content ,cols:30, rows: 5 %>
    <%= f.submit value = "投稿を保存する"%>

<% end %>